<?php if($queues !== false): ?>
<?php foreach($queues as $queue): ?>
	<?php
		$block =  $renderer->getNotificationBlockByQueue($queue);
		if($block !== false){
			echo $block;
		}
	?>
<?php endforeach; ?>
<?php else: ?>
	<div class="empty-feed">You have no <?php echo ($isFresh)?'fresh':'previous'; ?> notification</div>
<?php endif;?>